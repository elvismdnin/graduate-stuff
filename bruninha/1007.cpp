#include <iostream>

using namespace std;

int main()
{
//cria as variaveis
	int valora, valorb, valorc, valord, diferenca;
//pega as variaveis
	cin >> valora >> valorb >> valorc >> valord;
//efetua as variaveis
	diferenca = (valora*valorb) - (valorc*valord);
//mostra as variaveis
	cout << "DIFERENCA = " << diferenca << endl;
	return 0;
}
